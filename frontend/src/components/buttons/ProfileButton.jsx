import React from "react";
import { CircleUser } from "lucide-react";
const ProfileButton = () => {
  return <CircleUser  className="cursor-pointer hover:scale-105 transition-all transition-duration-300" height={40} width={40} />;
};

export default ProfileButton;
